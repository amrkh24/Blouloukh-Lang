namespace Blolokh.Compiler
{
    public enum TokenType
    {
        ياض, Print, Input, Assign, Plus, Minus, Star, Slash, If, EndBlock, Identifier, Number, EOF, NewLine, StartBlock, While, LeftParen, RightParen
    }
}